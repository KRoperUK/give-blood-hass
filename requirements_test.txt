-r requirements_dev.txt
pytest-homeassistant-custom-component>=0.13.290
# Exact pins: CI resolves these from this file, and scripts/check-tool-pins.sh
# asserts they match .pre-commit-config.yaml.
ruff==0.15.21
mypy==2.3.1
bandit==1.9.4
detect-secrets==1.5.0
pre-commit
# Docs site (Zensical). Kept here so `pytest tests/test_docs.py` and a local
# `zensical build` work from a single install.
zensical
python-dotenv
